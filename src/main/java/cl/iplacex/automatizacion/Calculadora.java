package cl.iplacex.automatizacion;

public final class Calculadora {
    public int sumar(int a, int b) { return Math.addExact(a, b); }
    public int restar(int a, int b) { return Math.subtractExact(a, b); }
}
